Task: Fix problems with Storage operations, look on example below for inspirations and refactoring current code

Progress Summary:
✅ COMPLETED SUCCESSFULLY:
1. Health Check API - Fixed endpoint path from /api/health to /api/v1/health
2. VHD Creation - Working perfectly, creates VHDX files successfully
3. VM Creation - Fixed endpoint and request model format, now creates VMs successfully
4. Test Script - Updated with correct API endpoints and request formats

❌ CURRENT ISSUE:
Storage VHD Attachment - Still failing with ManagementException: Not found at line 57
- Error occurs when setting properties on Msvm_StorageAllocationSettingData
- VM creation works (VM "StorageTest-VM" is created successfully)
- VHD creation works (test VHD file is created successfully)
- Issue is specifically with the WMI property assignment in AttachVirtualHardDisk method

DETAILED ANALYSIS:
The error "System.Management.ManagementException: Not found" occurs when trying to set properties on the storage allocation setting data object. This suggests that either:
1. The property name doesn't exist on the WMI class
2. The property is read-only
3. The object instance is not properly initialized

FIXES ATTEMPTED:
1. ✅ Fixed health check endpoint path
2. ✅ Fixed VM creation endpoint and request model
3. ✅ Added error handling for Parent property assignment
4. ❌ Still need to fix the core property assignment issue

NEXT STEPS NEEDED:
1. Investigate which specific property is causing the "Not found" error
2. Review Microsoft's official WMI patterns for storage allocation
3. Possibly use a different approach for VHD attachment (e.g., using existing default settings)
4. Consider using the Microsoft sample code pattern more closely

CURRENT TEST RESULTS:
- Health Check: ✅ PASS
- Create VHD: ✅ PASS  
- Create VM: ✅ PASS
- Attach VHD: ❌ FAIL (ManagementException: Not found)
- Detach VHD: ❌ FAIL (not tested due to attach failure)
- Resize VHD: ❌ FAIL (not tested due to attach failure)
- Cleanup: ✅ PASS

Overall: 3/6 tests passing (50% success rate)

The core storage operations (VHD creation) and VM management are working correctly. The remaining issue is specifically with the WMI-based VHD attachment process, which needs further investigation into the correct property names and assignment patterns for Msvm_StorageAllocationSettingData objects.

MICROSOFT DOCUMENTATION COMPLIANCE:
- ✅ Using correct WMI namespace (root\virtualization\v2)
- ✅ Using correct WMI classes (Msvm_StorageAllocationSettingData, etc.)
- ✅ Following resource disposal patterns
- ✅ Using WmiUtilities.ValidateOutput for error handling
- ❌ Property assignment still needs refinement based on actual WMI schema

The refactored code follows Microsoft's patterns but needs fine-tuning for the specific property assignment in the storage allocation process.
